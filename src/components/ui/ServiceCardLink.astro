---
import CategoryIcon from './CategoryIcon';
import StatusBadge from '../service/StatusBadge.astro';
import { accentStyleAttr } from '../../lib/categoryVisuals';
import type { ServiceCardFields } from '../../lib/serviceCard';

type Props = ServiceCardFields;

const { href, title, titleBn, description, domain, status, categoryId, icon } = Astro.props as Props;
const styleAttr = accentStyleAttr(categoryId);
---

<a class="service-card" href={href} style={styleAttr}>
  <div class="service-card__top">
    <CategoryIcon icon={icon} categoryId={categoryId} inheritAccent size={24} />
    <div class="service-card__titles">
      <h3 class="service-card__title">{title}</h3>
      <p class="service-card__title-bn" lang="bn">{titleBn}</p>
    </div>
    <StatusBadge status={status} />
  </div>
  <p class="service-card__description">{description}</p>
  <p class="service-card__domain">{domain}</p>
</a>
